
Object-patterns:

Character-Sprites:

          -objects
            -[*character-name]
            -[/--folder *character-name]
                -Animations / (scripts)
                -Magic / (scripts)
                -Projectiles / (scripts)
                -Weapons / (scripts)
                -Items / (scripts)
                -Controls / (scripts)

                *scripts are loaded through a ScriptLoader object.
